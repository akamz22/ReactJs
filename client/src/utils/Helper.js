// export function filterData(InputTxt, restaurants) {
//     const filteredData = restaurants.filter((allRestaurants) => allRestaurants?.data?.name?.toLowerCase().includes(InputTxt.toLowerCase()));
//     return filteredData;
// }


// export const filterData = ({e,allRestaurants}) => {
//     const filteredData = allRestaurants.filter((Restaurants) => {
//       if (e.target.value === "")
//         return allRestaurants;
//       console.log(e.target.value);
//       return Restaurants?.data?.name?.toLowerCase().includes(e.target.value.toLowerCase())
//     });
//     setSearchTxt(e.target.value);
//     setFilteredRestaurants(filteredData);
// }